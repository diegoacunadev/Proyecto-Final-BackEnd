import { Controller } from '@nestjs/common';

// Controlador base del módulo "users".
// Punto de entrada para las rutas relacionadas con usuarios.
@Controller('users')
export class UsersController {}
